name: Daily Intelligence Update

on:
  schedule:
    - cron: "0 8 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-intelligence-agents:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Feedstock Watchdog (mock)
        run: python agents/feedstock_watchdog.py

      - name: Run Regulatory Sentry (mock)
        run: python agents/regulatory_sentry.py

      - name: Run Capital Signals (mock)
        run: python agents/capital_signals.py

      - name: Run Technology Diligence (mock)
        run: python agents/technology_diligence.py

      - name: Run Market Access (mock)
        run: python agents/market_access.py

      - name: Run Community Pulse with Editor (mock)
        run: python agents/community_pulse.py

      - name: Commit updated data
        run: |
          git config user.name "SAF HUB Bot"
          git config user.email "bot@biojet.ng"
          git add data/
          git commit -m "📊 Daily intelligence update" || echo "No changes to commit"
          git push

      - name: Upload intelligence data artifacts
        uses: actions/upload-artifact@v4
        with:
          name: intelligence-data
          path: data/
          if-no-files-found: warn
